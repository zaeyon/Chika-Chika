const productionUri = 'https://api.chikachika-app.com';
const developmentUri = 'https://test-api.chikachika-app.com';

const serverConfig = {
  baseUri: developmentUri,
};

export default serverConfig;
